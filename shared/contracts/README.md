# API contracts

REST and Socket.IO API contracts used by services and clients.

Add as you define them:

- OpenAPI (e.g. `openapi.yaml`) for REST
- TypeScript types or JSON Schema for payloads
- Socket.IO event names and payload shapes

Keep one source of truth; generate client types or SDKs from it if needed.
