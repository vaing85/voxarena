# Supabase — get these from your project's API settings
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only key for privileged operations (battle creation, rating updates).
# NEVER expose this to the client.
SUPABASE_SERVICE_ROLE_KEY=

# Voice provider API keys (Phase 1 — add as providers are wired up)
ELEVENLABS_API_KEY=
OPENAI_API_KEY=
